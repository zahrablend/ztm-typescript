/* eslint-disable */
import { strict as assert } from "assert";

// Useful links:
// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/switch

let someNumber = parseInt("3");
const sample = "sample";
const someString = `${sample}`;

switch (someNumber) {
    case 1:
        break;
    case 2:
        break;
    default:
        // some code
}